'use babel';

import ReloadWorkspaceButtonView from '../lib/reload-workspace-button-view';

describe('ReloadWorkspaceButtonView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
